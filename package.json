{
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "devDependencies": {
    "parcel": "^2.0.0",
    "@parcel/transformer-sass": "2.8.0"
  },
  "scripts": {
    "watch": "parcel watch assets/scss/*.scss --dist-dir assets/css/ --public-url ./ --no-hmr",
    "build": "parcel build assets/scss/*.scss --dist-dir assets/css/ --public-url ./"
  }
}